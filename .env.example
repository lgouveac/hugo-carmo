# Variáveis de ambiente do CMS — configure-as no Vercel (Settings → Environment Variables).
# NUNCA comite os valores reais no Git.

# URL do projeto Supabase
SUPABASE_URL=https://gobslzsggskllmhzasti.supabase.co

# Chave service_role do Supabase (Settings → API). Fica SÓ no servidor (Vercel).
SUPABASE_SERVICE_ROLE=coloque-a-chave-service_role-aqui

# Login do painel /admin (defina os valores reais SÓ no Vercel, nunca aqui)
ADMIN_EMAIL=hugo.gcarmo@gmail.com
ADMIN_PASSWORD=defina-uma-senha-forte
